# Declaración de Uso de IA

Durante el desarrollo de este desafío utilicé Inteligencia Artificial (Gemini para la primera versión del código y la documentación, y Claude para una revisión crítica final del pipeline completo) como asistente para agilizar la escritura de código, la generación de gráficos y la estructuración de la documentación. Sin embargo, el análisis del negocio, la estrategia de validación y la toma de decisiones metodológicas fueron dirigidas y validadas por mí.

## Correcciones a outputs incorrectos o subóptimos de la IA

1. **Error de sintaxis y alucinación de librerías:** durante la generación del script de carga de datos inicial, la IA cometió un error de sintaxis escribiendo `pd.pd.read_csv()`, lo cual rompió el pipeline. Tuve que intervenir, hacer el *debugging* del error y corregir la llamada a la librería Pandas.
2. **Manejo de rutas relativas:** la IA sugirió cargar los archivos asumiendo que estaban en el directorio raíz (`train.csv`). Sin embargo, al revisar la estructura del repositorio cloneado, identifiqué que los datos estaban dentro de un subdirectorio. Tuve que corregir manualmente las rutas a `data/train.csv` y `data/test.csv` para asegurar la reproducibilidad del código.
3. **Profundidad del Análisis Exploratorio (EDA):** inicialmente, la IA generó el código de limpieza de datos pero omitió la documentación y justificación de por qué se tomaban esas decisiones (ej. tratamiento de nulos en jubilados). Tuve que solicitar explícitamente y guiar a la herramienta para que incluyera celdas de Markdown formales que tradujeran los hallazgos técnicos en *insights* de negocio auditables.
4. **Performance del modelo reportada sin cuestionar (el caso más importante):** el modelo LightGBM entrenado obtuvo un ROC-AUC de 0.9735 y un PR-AUC de 0.9089 en la validación Out-of-Time. Al pedirle a la IA que revisara el notebook final, señaló que este nivel de performance es atípicamente alto para un modelo de riesgo de crédito de consumo (la industria suele ubicarse en un rango de 0.70–0.85), y que debía descartarse fuga de información antes de dar por buena la cifra — algo que mi primera versión del análisis no cuestionaba, dando el resultado por bueno directamente. A partir de esa observación, agregué en el notebook `02_Modelado_y_Evaluacion.ipynb` (sección "2.1 Validación crítica") una investigación explícita: comparé la distribución de las variables `score_buro` y `num_contactos_ult_trimestre` entre clientes con y sin default, y revisé la correlación de cada feature con el target, para descartar que alguna contuviera información posterior al desembolso (ej. señales de cobranza que solo existen porque el cliente ya cayó en mora). Documenté ambos escenarios posibles (con y sin fuga de información) y dejé la recomendación de monitorear de cerca el desempeño real en producción dado lo inusual del resultado, en vez de simplemente reportar el número más favorable.

## Elementos validados manualmente

- Se validó de forma estricta que la lógica de cálculo económico del producto respetara la fórmula entregada (LGD del 55% y cálculo de intereses) antes de confiar en el bucle de optimización del umbral.
- Se verificó que la partición *Out-of-Time* (noviembre 2024) respetara la línea temporal y no generara filtración de datos (*data leakage*) hacia el conjunto de entrenamiento.
- Se verificó manualmente que las columnas generadas por *One-Hot Encoding* en `train` y `test` coincidieran entre sí (ver sección 4 del notebook 1); dado que ambos sets se codifican por separado, se agregó una alineación explícita de columnas (`reindex`) en vez de asumir que ambos procesos generarían las mismas columnas por casualidad.
- Se revisó que la sección de explicabilidad del notebook 2 reportara el `id_solicitud` real de cada cliente (y no la posición de la fila en la matriz), y que la clase predicha fuera consistente con el umbral de negocio recomendado (0.40) y no con un umbral genérico de 0.5.
